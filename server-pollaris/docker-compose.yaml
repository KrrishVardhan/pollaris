services:
  pollarisdb:
    image: postgres:17
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: krrish
      POSTGRES_PASSWORD: pollaris
      POSTGRES_DB: pollaris

    volumes:
      - "pollaris_data:/var/lib/postgresql/data"
volumes:
  pollaris_data: